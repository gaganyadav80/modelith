name: modelith
description: One annotation for Dart data models — @Model gives you fromJson/toJson, copyWith and value equality in a single generated part file.
version: 0.1.0
repository: https://github.com/gaganyadav80/modelith

environment:
  sdk: ^3.10.0

dependencies:
  equatable: ^2.1.0
  json_annotation: ^4.12.0
  meta: ^1.19.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
